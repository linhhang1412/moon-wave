{
  "name": "@moon-wave/core",
  "version": "0.1.0",
  "description": "Core agent loop and tool registry for moon-wave",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@moon-wave/types": "workspace:*",
    "@moon-wave/providers": "workspace:*",
    "@moon-wave/memory": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
